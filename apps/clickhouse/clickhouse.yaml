apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hyperdx
  namespace: flux-system
spec:
  releaseName: hyperdx
  chart:
    spec:
      chart: hdx-oss-v2
      version: "0.8.0"
      sourceRef:
        kind: HelmRepository
        name: hyperdx-charts
        namespace: flux-system
  install:
    createNamespace: false